# A web application to manage the Oompa Loompa's crew of Willy Wonka's chocolate factory.

- Steps to run the application:

1. Clone the project from GitHub into a local directory:
  git clone https://github.com/lcastro19/naptilus-test.git

2. Navigate to the directory and open the command prompt or terminal, depending on your operating system.

3. Install project dependencies:
   ```
   npm install
   ```

4. Run the application:
   ```
   npm run dev
   ```

5. The application will run locally at the following URL:
   [http://localhost:5173]